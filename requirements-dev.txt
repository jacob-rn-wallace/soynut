# Power of 10, Rule 10: static analysis tooling for this project's Python
# files (tools/, font-tables/, roms/, Arduino */convert_images.py).
# Install with: pip3 install -r requirements-dev.txt
# Run with:     ruff check . && mypy .
ruff
mypy
types-pyserial
